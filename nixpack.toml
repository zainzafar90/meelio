# nixpacks.toml
[phases.setup]
nixpkgs = ["nodejs_22", "yarn"]

[phases.install]
dependsOn = ["setup"]
cmds = ["yarn install --frozen-lockfile", "npm install -g serve"]

[phases.build]
dependsOn = ["install"]
cmds = ["yarn build:web"]

[start]
cmd = "serve -s apps/web/dist -p $PORT"
